import React from 'react';
import ReactDOM from 'react-dom/client';
import { BigExpertApp } from './BigExpertApp';

import './styles.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <BigExpertApp />
  </React.StrictMode>
);
